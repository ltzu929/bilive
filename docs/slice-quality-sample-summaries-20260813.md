# 2026-08-13 样本时间摘要

这些摘要由源录播的 `ffprobe` 时长和 XML 弹幕时间戳生成。它们只用于内部预筛，不是 MiMo 结论，也不是人工 `keep/review/drop` 标签；没有生成长候选视频。

## 近期源录播

| 样本 | 弹幕高密度窗口摘要 | 当前内部信号 | 下一步 |
|---|---|---|---|
| `8792912_20260812-10-57-07` | `140–150s` 主要是“晚上好/老板大气”；`40–50s` 主要是表情、揉揉；`180–190s` 仍以问候为主 | 强噪声候选；与已确认垃圾预览同源 | 不生成预览；必要时只看高密度窗口附近是否存在音频节目效果 |
| `8792912_20260811-12-30-28` | `260–270s`、`140–150s`、`540–550s` 都出现连续笑声/表情包 | 可能有笑点，但主线和落点未知 | 看通过闸门的 trim；通常先看 15–60 秒，较长但结构完整也保留给人工判断 |
| `8792912_20260812-10-55-18` | `0–20s` 主要是“好耶/打卡”；`50–60s` 有打卡、感谢灯牌 | 低信息开头，暂不支持保留 | 先标记低优先级，不降低检测阈值 |
| `8792912_20260812-11-03-05` | `240–250s` 主要是“2222/1212”；`1160–1170s` 有“何意味/草”；`750–760s` 表情和感谢 | 弹幕活跃但语义弱，典型 `danmaku_noise` 候选 | 不把热度当节目效果；需要媒体核验 |
| `8792912_20260812-11-53-06` | `10–20s` 出现“加码”“跟个 npc 一样”；`90–110s` 表情包和笑声 | 有可能存在互动主线，但上下文不足 | 查是否有完整事件，而不是只截反应 |
| `8792912_20260812-11-56-04` | `110–120s` 有连续笑声、“蛙趣/救命”；`90–100s` 出现“权杖概率这么高” | 可能是抽奖/礼物话题，也可能是错误关键词 | 重点核验真实节目效果是否另有主线 |
| `8792912_20260812-11-59-12` | `30–40s` 主要是老板大气/感谢礼物；`0–20s` 有笑声、电影票和感谢留言 | 送礼感谢与普通互动噪声偏强 | 不自动保留；查是否有独立笑点 |
| `8792912_20260812-12-06-04` | `300–310s`、`580–590s` 主要是连续笑声；`330–340s` 主要是表情和“可爱” | 有情绪反应线索，但没有语义主线 | 必须听看前因和收束，不能只靠弹幕 |
| `8792912_20260812-13-04-59` | `20–40s` 明确出现“晚安、早日康复、好梦”；`0–10s` 也以晚安为主 | 强负样本候选：普通下播/晚安 | 可作为 `ordinary_goodbye` 重点核验样本，不生成预览 |
| `22384516_20260811-14-56-33` | `220–230s`、`210–220s` 主要是晚安/辛苦；`70–80s` 有问号、揉揉和 `kusa` | 结尾疑似普通下播，但中段可能有独立片段 | 只核验 `70–80s` 和结尾两个短时间段 |
| `22384516_20260811-14-03-35` | `170–180s` 主要是礼物感谢；`420–430s` 有笑声/表情；`190–200s` 有一条较长观众留言 | 可能存在互动主线，但高峰也被感谢消息占据 | 核验长留言是否引出完整回应 |
| `22384516_20260812-12-58-02` | `0–40s` 主要是好耶、晚上好、礼物感谢和“去个洗手间” | 强低价值候选；与已完成检测中无 burst 的样本一致 | 保持 `pending`，作为低弹幕漏检对照，不直接调阈值 |

## 预筛结论

- 明显值得优先核验的不是“弹幕最多”的窗口，而是 `8792912_20260811-12-30-28` 的连续笑声、`8792912_20260812-11-53-06` 的互动句子，以及 `22384516_20260811-14-03-35` 的长留言回应。
- `8792912_20260812-13-04-59`、`22384516_20260811-14-56-33` 的晚安窗口和 `22384516_20260812-12-58-02` 的欢迎/感谢窗口，适合作为负样本或低价值对照，但最终仍需按视频和音频确认。
- `8792912_20260812-11-56-04` 专门用于验证“模型抓到抽奖/礼物关键词，但真实节目效果可能另有主线”这一失败类型。
- 目前没有证据支持直接降低 `burst_ratio=3.0`。先完成这些样本的人工标签，再区分“检测漏检”和“本来没有值得切的内容”。

## 首轮隔离探针结果

以下结果来自隔离副本，上传已关闭；候选窗口只用于内部处理，没有作为用户预览展示。

| 样本 | 检测结果 | 模型/质量结果 | 当前解释 |
|---|---|---|---|
| `8792912_20260811-12-30-28` | `3.0x / 10s` 检出 1 个候选，内部窗口 `141–381s`，爆点核心 `261–265s` | MiMo 返回 1 个结果，但 trim 超出候选时长，`judge_failed`；未生成预览 | 记录为“模型边界非法”样本，不能据此判断内容本身应 `keep` 或 `drop` |
| `8792912_20260812-11-56-04` | `3.0x / 10s` 未检出候选 | 未进入 MiMo | 保留为“低弹幕/无候选”待人工核验样本 |
| `8792912_20260812-13-04-59` | `3.0x / 10s` 未检出候选 | 未进入 MiMo | 已知内容偏普通问候/感谢，作为“无候选且低价值”对照，不足以证明阈值漏检 |

这轮只验证了流程行为和失败类型，没有替代人工 `keep/review/drop` 标签。下一轮优先核验 `11-53-06`、`11-56-04`、`14-03-35` 的短时间摘要/短片，再用 `13-04-59` 和 `22384516_20260812-12-58-02` 做负样本对照；在此之前不调整弹幕检测阈值。

## 第一批真实流程结果

本批使用近期源录播的隔离副本，`BILIVE_AUTO_UPLOAD=0`、`BILIVE_SKIP_UPLOAD_QUEUE=1`。以下是机器结果，不是人工标签；没有通过闸门的内部候选均未展示为用户预览。

| 样本 | 机器结果 | 当前失败类型 |
|---|---|---|
| `8792912_20260812-11-53-06` | MiMo 返回空片段 | 弹幕有反应，但模型认为是碎碎念，缺乏完整事件 |
| `22384516_20260811-14-56-33` | MiMo 返回空片段 | 候选内话题松散，模型未找到独立主线 |
| `22384516_20260811-14-03-35` | `3.0x` 下无候选 | 热度不足以触发模型，仍待人工确认是否漏检 |
| `8792912_20260812-10-57-07` | MiMo 返回空片段 | 已知旧垃圾预览的源录播，新流程没有再生成预览 |
| `22384516_20260811-14-11-50` | MiMo 返回空片段 | 弹幕对应多个零散问答，没有完整叙事 |
| `8792912_20260811-12-05-45` | 首次返回合法 trim 但分数非法；明确分数范围后再次返回空片段 | 分数格式异常已进入提示词修复；内容仍未被模型确认可投稿 |
| `8792912_20260811-11-38-34` | MiMo JSON 解析失败 | 模型返回格式失败，未生成预览 |

因此本批暂时没有可交给人工观看的最终短片；这不是把 240 秒候选递给用户的理由。下一批继续从默认阈值下有候选的近期录播中取样，目标是获得至少一条通过闸门的真实 trim，再开始收集人工质量标签。

## 上下文边界隔离对照

为验证候选上下文是否造成空判，未修改生产默认值，只在隔离副本上对同一录播做了对照：

- `22966160_20260808-12-23-00` 使用 `±60s` 时，MiMo 找到一条 `quality=0.90 / completeness=0.95 / confidence=0.90` 的“变身失败/公主抱”候选，但 trim 为 `30–120s`，贴候选结束边界，进入 `review`，没有预览。
- 同一录播使用 `±90s` 或默认 `±120s` 时，MiMo 返回空片段；这说明上下文长度会影响模型判断，但目前没有人工真值支持直接改默认值。
- `22966160_20260808-12-40-12` 使用 `±60s` 时找到两条候选，其中“世界第一偶像/地位”候选分数为 `0.95 / 0.90 / 0.95`，但 trim 为候选起点 `0–80s`，进入 `review`；另一条因爆点覆盖校验失败而拒绝。
- 将该事件扩展为约 `100–220s` 的聚焦隔离候选后，MiMo 仍返回高分候选，但 trim 为 `0–75s`，继续被边界闸门拦截。

这组结果确认当前优先问题是“候选起点缺少事件前因”，而不是 60 秒长度限制。暂不放宽边界闸门，也不把这些 `review` 候选交给人工当作最终预览。
