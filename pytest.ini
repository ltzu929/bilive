[pytest]
testpaths = tests
addopts = --import-mode=importlib -m "not integration"
markers =
    integration: tests that require real API keys, cloud SDKs, real media paths, or external services
    legacy: compatibility tests for retained legacy entry points that are not the recommended daily workflow
